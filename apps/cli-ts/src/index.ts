#!/usr/bin/env bun
import("./cli/index");

// import { ensureRagServerRunning } from "@utils/ensureRagServer";

// console.log("codr CLI is alive.");

// const ok = await ensureRagServerRunning();

// if (ok) {
//   // ✅ Proceed with CLI only if server is ready
//   await import("./cli/index");
// } else {
//   // ❌ Optional: Exit with code
//   process.exit(1);
// }


